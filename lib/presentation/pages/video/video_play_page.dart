import 'package:flutter/material.dart';

class VideoPlayPage extends StatelessWidget {
  static String name = "video_play";
  const VideoPlayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}